const doc = document;

this.window.addEventListener('load', e => {
    window.addEventListener('message', e => {
        switch (e.data.action) {
            case 'updateInfo':

            break;
        }
    })
})